Kalmann Filtered Data
========================

Outputs Kalmann filtered orientation data (Yaw,pitch,roll)
over the serial port. The microcontroller only sends the data
if the correct character is received over serial.